# Адрес backend API (см. ../backend). Для локальной разработки —
# uvicorn на 127.0.0.1:8000 (см. ../backend/README.md).
VITE_API_BASE_URL=http://127.0.0.1:8000

# Логин бота без @, для ссылки "т.me/<bot>?start=..." на экране «Профиль»
# (см. bot/config.py — то же имя, что регистрировали в @BotFather).
VITE_BOT_USERNAME=chtivo_shop_bot
