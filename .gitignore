# Общие для всех подпроектов — каждый (backend/, bot/, miniapp/) держит
# ещё и свой собственный .gitignore рядом с кодом; этот — только на то,
# что может оказаться прямо в корне репозитория.
.DS_Store
Thumbs.db
*.log
/tools/
